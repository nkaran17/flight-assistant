import { MissingTextPipe } from './missing-text.pipe';

describe('MissingTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MissingTextPipe();
    expect(pipe).toBeTruthy();
  });
});
